<Window x:Class="App_Control_Servo_Press_Delta.Popup.Chart_Report"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:App_Control_Servo_Press_Delta"
    xmlns:oxy="http://oxyplot.org/wpf"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    AllowsTransparency="True"
    Background="Transparent"
    WindowState="Maximized"
    WindowStyle="None"
    mc:Ignorable="d"  >
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="Gray"
            Opacity="0.5">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDown">
                    <i:InvokeCommandAction Command="{Binding GridClickCommand}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Grid>
        <materialDesign:Card
            HorizontalAlignment="Center" Height="600" Width="1000"
            materialDesign:ElevationAssist.Elevation="Dp16"
            UniformCornerRadius="5" x:Name="FocusBorder" RenderTransformOrigin="0.5,0.5">
            <materialDesign:Card.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform AngleY="0"/>
                    <RotateTransform/>
                    <TranslateTransform Y="0"/>
                </TransformGroup>
            </materialDesign:Card.RenderTransform>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock x:Name="tb_Title" FontFamily="Source Sans Pro" FontSize="20" FontWeight="Bold" TextAlignment="Center" Margin="10"></TextBlock>
                <Grid Grid.Row="1">
                    <oxy:PlotView x:Name="plotView1" Background ="Transparent" />
                </Grid>
            </Grid>

        </materialDesign:Card>
    </Grid>
</Window>
